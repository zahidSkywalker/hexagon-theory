services:
  - type: web
    name: hexagon-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: DB_NAME
        value: hexagon_db
      - key: SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        value: https://hexagon-theory.vercel.app
